{
  "name": "cafe-mapping-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "deploy:infra": "turbo run deploy:infra --filter=@cafemap/infra"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
